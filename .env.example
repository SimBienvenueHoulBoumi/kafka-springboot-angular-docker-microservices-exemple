# ⚠️ IMPORTANT: Copiez ce fichier vers .env et remplissez les valeurs
# cp .env.example .env
# Ne commitez JAMAIS le fichier .env dans Git !

# ==========================================
# PostgreSQL - Users Database
# ==========================================
DB_USERS_HOST=postgres-users
DB_USERS_PORT=5432
DB_USERS_NAME=users_db
DB_USERS_USERNAME=postgres
DB_USERS_PASSWORD=CHANGEZ_MOI_EN_PRODUCTION

# PostgreSQL - Users Database (pour le service PostgreSQL)
POSTGRES_USERS_USER=postgres
POSTGRES_USERS_PASSWORD=CHANGEZ_MOI_EN_PRODUCTION
POSTGRES_USERS_DB=users_db

# ==========================================
# PostgreSQL - Tasks Database
# ==========================================
DB_TASKS_HOST=postgres-tasks
DB_TASKS_PORT=5432
DB_TASKS_NAME=tasks_db
DB_TASKS_USERNAME=postgres
DB_TASKS_PASSWORD=CHANGEZ_MOI_EN_PRODUCTION

# PostgreSQL - Tasks Database (pour le service PostgreSQL)
POSTGRES_TASKS_USER=postgres
POSTGRES_TASKS_PASSWORD=CHANGEZ_MOI_EN_PRODUCTION
POSTGRES_TASKS_DB=tasks_db

# ==========================================
# Kafka Configuration
# ==========================================
KAFKA_BOOTSTRAP_SERVERS=kafka-1:19092,kafka-2:19092,kafka-3:19092
KAFKA_CLUSTER_ID=MkU3OEVBNTcwNTJENDM2Qk

# ==========================================
# JWT Configuration
# ==========================================
# ⚠️ IMPORTANT: Utilisez une clé secrète forte en production (minimum 256 bits)
# Générez une clé avec: openssl rand -base64 32
JWT_SECRET=CHANGEZ_MOI_EN_PRODUCTION_générez_une_clé_forte_avec_openssl_rand_base64_32

# ==========================================
# Admin User
# ==========================================
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=CHANGEZ_MOI_EN_PRODUCTION
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User
